language: python

os:
  - linux

python:
  - "2.7"
  - "3.4"
  - "3.8"

cache: pip

env:
  - DJANGO_VERSION=2
  - DJANGO_VERSION=3

install:
  - pip install -r requirements.txt

script:
  - coverage run --source='.' manage.py test && coverage report -m --skip-covered

after_success:
  - coveralls
